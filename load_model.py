from keras.models import load_model
def load_model(path):
    """
    Loads a Keras Sequential model from the given path.

    Parameters:
        path (str): The path to the saved model.

    Returns:
        The loaded Keras Sequential model.
    """
    return load_model(path)

